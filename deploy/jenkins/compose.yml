name: clinicflow-jenkins

services:
  controller:
    build:
      context: ./controller
    image: clinicflow/jenkins-controller:2.568.1
    restart: unless-stopped
    environment:
      CASC_JENKINS_CONFIG: /var/jenkins_casc/jenkins.yaml
      CASC_STRICT_SECRET_RESOLUTION: "true"
      JAVA_OPTS: >-
        -Djenkins.install.runSetupWizard=false
        -Dhudson.plugins.git.GitSCM.ALLOW_LOCAL_CHECKOUT=true
      CLINICFLOW_JENKINS_SCM_URL: ${CLINICFLOW_JENKINS_SCM_URL:-file:///workspace/clinicflow}
      CLINICFLOW_JENKINS_BRANCH: ${CLINICFLOW_JENKINS_BRANCH:-*/fix/engineering-stabilization}
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - jenkins-home:/var/jenkins_home
      - ./casc:/var/jenkins_casc:ro
      - ./secrets/admin-password:/run/secrets/admin-password:ro
      - ./secrets/agent_ed25519:/run/secrets/agent_ed25519:ro
      - ../..:/workspace/clinicflow:ro
    networks:
      - jenkins-internal
    depends_on:
      agent:
        condition: service_started

  agent:
    build:
      context: ./agent
    image: clinicflow/jenkins-agent:7.13.0
    restart: unless-stopped
    environment:
      JENKINS_AGENT_SSH_PUBKEY_FILE: /run/secrets/agent_ed25519.pub
    group_add:
      - "${DOCKER_GID:-0}"
    volumes:
      - ./secrets/agent_ed25519.pub:/run/secrets/agent_ed25519.pub:ro
      - ../..:/workspace/clinicflow:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - jenkins-agent-workspace:/home/jenkins/agent
      - maven-cache:/home/jenkins/.m2
      - npm-cache:/home/jenkins/.npm
      - playwright-cache:/ms-playwright
    networks:
      - jenkins-internal

networks:
  jenkins-internal:
    internal: true

volumes:
  jenkins-home:
  jenkins-agent-workspace:
  maven-cache:
  npm-cache:
  playwright-cache:
